pipeline {
  agent any
  stages {
    stage('Install ') {
      steps {
        echo 'To install dependencies'
      }
    }
    stage('Build') {
      steps {
        echo 'make build'
      }
    }
  }
}